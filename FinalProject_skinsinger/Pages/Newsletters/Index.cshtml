@page
@model FinalProject_skinsinger.Pages.Newsletters.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Newsletter[0].Email)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Newsletter) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Email)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.NewsletterId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.NewsletterId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.NewsletterId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
